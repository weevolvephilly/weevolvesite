{%- for about in collections.about -%}
    <div class="container">
        <h2 class="header">{{ about.data.title }}</h2>
        <article class="content">{{about.templateContent | safe }}</article>
    </div>
{% endfor %}